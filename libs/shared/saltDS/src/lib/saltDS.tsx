import { Button } from '@salt-ds/core';

export function SaltDS() {
  return (
      <Button sentiment="accented" appearance="solid">
        Welcome to salt DS
      </Button>
  );
}

export default SaltDS;
